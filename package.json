{
  "scripts": {
    "deploy": "yarn build&&firebase deploy --only functions --project=prod",
    "deploydevelop": "yarn build&&firebase deploy --only functions --project=develop"
  },
  "devDependencies": {
    "firebase-tools": "^9.9.0"
  }
}
