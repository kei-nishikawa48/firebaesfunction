{
  "scripts": {
    "deploy": "firebase use function-test-48 && firebase deploy --only functions",
    "deploydevelop": "firebase use function-test-48x && firebase deploy --only functions"
  },
  "devDependencies": {
    "firebase-tools": "^9.9.0"
  }
}
