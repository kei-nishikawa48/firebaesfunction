{
  "scripts": {
    "deploy": "firebase login && firebase use function-test-48 && firebase deploy --only functions",
    "deploydevelop": "firebase login && firebase use function-test-48x  && firebase deploy --only functions"
  },
  "devDependencies": {
    "firebase-tools": "^9.9.0"
  }
}
